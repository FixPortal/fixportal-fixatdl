{
  "$comment": [
    "PR review risk tiering. Read by ~/.claude/hooks/pr-review-policy.sh.",
    "Any file matching 'high' => HIGH (Gitar + CodeRabbit). All files matching 'low' => LOW.",
    "Everything else => NORMAL (Gitar only). No match anywhere => NORMAL, deliberately.",
    "",
    "This repo PUBLISHES a package (release.yml), so its dependency manifests are supply",
    "chain in both directions - what it consumes and what consumers then inherit.",
    "LOW justification for THIS repo: docs/ is developer documentation with no site build.",
    "Root *.md is deliberately NOT low: README.md is packed into the published nupkg",
    "(PackageReadmeFile in src/FixPortal.FixAtdl/FixPortal.FixAtdl.csproj), so markdown",
    "ships in the artefact and tiers NORMAL.",
    "",
    "Dependency manifests are deliberately NOT in 'high' and must not be added (estate rule,",
    "2026-07-29) - package.json, package-lock.json, Directory.Packages.props, **/*.csproj. They are",
    "what Dependabot edits, and dependency PRs are out of AI code review on both vendors:",
    "CodeRabbit refuses bot authors outright and Gitar's bot review is switched off, because",
    "reviewing every bump exhausts the allowance and starves the changes that need it. Listing",
    "them HIGH would demand a reviewer that can never run - coverage on paper, none in fact. CI",
    "plus Dependabot's own advisory data is what gates a bump. nuget.config, global.json, .npmrc",
    "and Directory.Build.props stay HIGH: registry/feed and analyzer config, not bot-edited.",
    "",
    "'.github/workflows/**' LEFT the high list on 2026-08-19 and is asserted in",
    "review-policy-guard.yml instead, on the same reasoning that removed .gitignore on",
    "2026-08-02: the hook tiers by path glob, so it cannot tell a permissions one-liner from",
    "a rewrite of the deploy job, and bills a metered CodeRabbit review either way. Measured",
    "over 100 HIGH PRs in the preceding 30 days: config-only diffs were 27 of them and",
    "produced FOUR actionable comments between them, against 174 from code-bearing diffs,",
    "while 45 of the 100 got no CodeRabbit verdict at all - throttled out at 73 reviews per",
    "7 days against a 30/7d threshold. The glob was not buying review of workflow changes;",
    "it was crowding out review of code.",
    "",
    "Do NOT re-add it without first removing the corresponding assertions from the guard.",
    "Deleting the guard itself needs no reviewer: \"Review policy intact\" is a REQUIRED",
    "status check, so removing the workflow means the check never reports and no PR merges.",
    "",
    "The coverage-floor checker and its verifier run from the PR's own checkout. Both stay",
    "HIGH because a PR that neuters them can report a green CI Gate while enforcing no floor;",
    "review-policy-guard.yml asserts these exact paths remain protected.",
    "",
    "docs/ai-findings.md is deliberately NOT under a low glob: it is the running ledger of",
    "reviewer findings and their dispositions, so edits to it tier NORMAL (the no-match",
    "default). LOW needs every changed file to match a low glob and the glob engine has no",
    "exclusions, so 'low' enumerates the docs subtrees and remaining root files instead of",
    "'docs/**'. A NEW root-level docs file also tiers NORMAL until listed here - the safe",
    "direction, since unknown risk is not low risk."
  ],

  "version": 1,

  "high": [
    ".github/workflows/review-policy-guard.yml",
    ".github/scripts/assert_workflow_hygiene.py",
    ".github/scripts/assert_gate_coverage.py",
    "scripts/assert-coverage-floor.ps1",
    "scripts/test/verify-coverage-floor.ps1",
    ".github/workflows/ci.yml",
    ".claude/review-policy.json",
    ".coderabbit.yaml",
    ".config/dotnet-tools.json",
    "nuget.config",
    "global.json",
    ".npmrc",
    "Directory.Build.props"
  ],

  "low": [
    "docs/architecture/**",
    "docs/superpowers/**",
    "docs/batch-3-findings-disposition.md",
    "docs/batch-5-conformance-review.md",
    "docs/coverage-baseline.md",
    ".vscode/**"
  ]
}
